import 'package:flutter/material.dart';

class BilledeUpload extends StatelessWidget {
  const BilledeUpload({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('BilledeUpload'),
      ),
      body: const Center(
        child: Text('This is the BilledeUpload page'),
      ),
    );
  }
}